extends ../layout
block content
	.w-1200
		h1= title
		.msg
			span 发布者：#{creator}
			span= creatime
		.content= content
		.operat
			- if(iftake)
				a(href="/task.take") 放弃任务
			- else
				a(href="/task.take") 接受任务
			- if(uname == creator)
				a(href="#") 指定成员
		